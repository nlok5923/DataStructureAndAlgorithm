//using inbuilt stack
#include<bits/stdc++.h>
#include<stack>
int main()
{
    stack<int> s;
    
    s.push(10);s.push(20);s.push(30);s.push(40);s.push(50);
    s.pop();//will delete value;
    s.top();//will give the top most value 
    s.empty();//to check if stack is empty
    
    
}